<div class="user_form">
	User email: <%= @user.email%>
  <%= form_for @user do |f| %>

  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meeting_thread.errors.count, "error") %> prohibited this meeting_thread from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%if @user.type != 'Turker'%>
  <div class="field">
    <%= f.label :timezone %><br />
    <%= f.time_zone_select :default_time_zone %>
  </div>
<%end%>
  <%= f.submit 'Update' %>

  <br/>
  <%end%>


</div>